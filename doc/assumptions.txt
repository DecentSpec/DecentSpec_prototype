for miner:
    1 currently ignore digital signature
    2 currently use flask builtin http server (low performance actually)
    3 [DONE] currently is permanent registration, leasing is to be implemented 
    4 [DONE] currently no reward implementation - need add pub key gen module first
    5 more exception raise
    6 need to use formatted logger
    7 eliminate duplicate data-based local updates in one block
    8 too long time to for PoW when large pool, need a pre-bundling of localweights